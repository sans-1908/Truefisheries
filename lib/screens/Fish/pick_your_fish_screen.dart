import 'package:flutter/material.dart';

class PickYourFishScreen extends StatelessWidget {
  const PickYourFishScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0XFF73D2AB),
      appBar: AppBar(
        backgroundColor: const Color(0XFF0A3E45),
        title: const Text('Pick your fish'),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
